<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout style="@style/about_About.wrapper" android:id="@+id/about_AboutLinearLayout">

        <LinearLayout style="@style/about_sub_wrapper">

            <ImageView
                android:layout_width="wrap_content"
                android:layout_height="100dp"
                android:layout_gravity="center_horizontal"
                android:layout_marginBottom="24dp"
                android:layout_weight="0.17"
                android:src="@drawable/logo" />

            <TextView
                android:id="@+id/description"
                style="@style/about_description" />
        </LinearLayout>

        <View style="@style/about_separator" />

        <LinearLayout
            android:id="@+id/about_providers"
            style="@style/about_sub_wrapper"
            android:padding="0dp"></LinearLayout>
    </LinearLayout>
</ScrollView>